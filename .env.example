# Server only. Never prefix these with VITE_. Keep .env.local out of source control.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash-preview
# Choose a long random private access token; enter it in the advisor's connection panel.
RFL_ADVISOR_TOKEN=
